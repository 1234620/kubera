# Analytics queries

One file per metric. Every file opens with a header comment stating purpose,
inputs, output grain, the units of each measure, and the formula in words.
Rules: [`../../rules/SQL.md`](../../rules/SQL.md). Specs:
[`../../docs/07-analytics-spec.md`](../../docs/07-analytics-spec.md).

Populated in stages 3–6.
