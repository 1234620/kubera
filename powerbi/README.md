# Power BI

`slb_desk.pbit` — a template, so the model and visuals are versioned without the
data. Connection details: [`../docs/10-runbook.md`](../docs/10-runbook.md).

`measures.md` lists every DAX measure against the SQL metric it mirrors, so the
report and the database cannot drift apart silently.

Built in stage 9.
