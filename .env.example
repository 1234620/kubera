# Development defaults. Localhost only — the Postgres port is not published
# outside the compose network, and this is not where a real secret would go.
POSTGRES_DB=slbdesk
POSTGRES_USER=slbdesk
POSTGRES_PASSWORD=slbdesk
POSTGRES_HOST=db
POSTGRES_PORT=5432
API_PORT=8000
INGEST_DAYS=30
